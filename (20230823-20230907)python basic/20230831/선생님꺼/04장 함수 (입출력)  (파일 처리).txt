04장 함수 (입출력) / (파일 처리)
- 지금까지 공부한 내용을 바탕으로 (02-03: 프로그래밍 기초) 
- 입출력은 프로그래밍 설계와 관련이 있음
- 프로그래머(개발자, 코더)는 프로그램을 만들기 전에 어떤 식으로 동작하게 할 것인지 설계부터 하는데 ( -> 순서도(의사코드 포함), 요구분석서 )
- 특정 프로그램만 사용 / 모든 프로그램이 공통적으로 사용 / 오픈 API로 공개 (???)

함수를 사용해야하는 이유?
- 똑같은 내용을 반복 작성하는 경우
 - 반복되는 부분 중에서 가치있는 부분을 한 뭉치로 묶어(함수로 작성) 
  !!! 입력값을 주었을때 어떤 결과값을 리턴해준다 라는 식의 함수로 작성하는것이 중요!!!
- 기능 단위의 함수로 분리해 놓으면 프로그램 흐름을 일목요연하게 볼수 있다
 - 원재료가 여러 공정을 거쳐서 완제품이 거치는 것처럼, 입력한 값이 여러 함수를 거치면서 원하는 결괏값을 내는것

함수() - 머리(header)                / 몸통 (body)
             함수(변수)명                 / 동작 과정(절차) - 코드
             매개 변수(parameter)  / 반환값(return)

def 함수명(매개변수):           4가지 함수X ->  매개변수, 반환값은 생략이 가능하다
....수행할 문장1
....수행할 문장2
....[return] 

함수호출
변수 = 함수명(인수1, 인수2)     인수(arguments)